// No fixed seed data for v1 — sign up a developer through /auth/signup and
// create an application through the dashboard instead. Kept as an entry
// point for future fixtures (e.g. demo data for staging).
async function main() {
  console.log("Nothing to seed yet — use /auth/signup to create your first developer account.");
}

main();
